<div class="${pdict.componentClasses}" style="${pdict.componentStyles}">
    <isinclude url="${URLUtils.url('Tile-Show', 'pid', pdict.product.ID, 'swatches', pdict.swatches, 'ratings', pdict.ratings,
                    'showQuickView', pdict.quickView)}" />
</div>
