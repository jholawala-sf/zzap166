<script src="${pdict.loadJsUrl}"></script>

<script>
    $Lightning.use("${pdict.appName}", function() {                  
            $Lightning.createComponent("${pdict.cmptName}", 
                { },                  
                "cmptHere",
                function(cmp) {
                    // do something if want
                }
            );
        },
        '${pdict.communityUrl}'
    );
</script>

<div id="cmptHere"></div>