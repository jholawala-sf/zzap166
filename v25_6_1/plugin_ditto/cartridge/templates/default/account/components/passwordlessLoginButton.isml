<isinclude template="account/password/requestPasswordlessLoginModal" />
<div class="form-group">
    <a href="${URLUtils.url('PasswordlessLogin-Auth', 'reentryEndpoint', pdict.oAuthReentryEndpoint || 1)}" data-toggle="modal" data-target="#request-passwordless-login-modal"
        id="passwordless-login-modal" class="btn btn-block btn-dark btn-passwordless" role="button" aria-pressed="true">
        ${Resource.msg('link.passwordless.login', 'login', null)}
    </a>
</div>
