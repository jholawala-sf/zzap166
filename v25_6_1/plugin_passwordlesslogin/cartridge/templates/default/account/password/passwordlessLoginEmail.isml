<body>
    <isif condition="${pdict.firstName}">
        <p>${Resource.msg('msg.passwordless.login.dear', 'login', null)}&nbsp;${pdict.firstName},</p>
    </isif>

    <p>${Resource.msg('msg.passwordless.login.message1', 'login', null)}</p>

    <h1>${pdict.passwordlessToken}</h1>

    <p><i>${Resource.msg('msg.passwordless.login.message2', 'login', null)}</i></p>
</body>
